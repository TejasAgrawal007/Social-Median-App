<!doctype html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="../stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <main class="min-h-screen w-full bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-green-400 hover:bg-green-500 px-4 py-2  rounded-lg font-semibold" href="/profile">Profile</a>
          </div>
        <form action="/update/<%= post._id %>" method="post">
            <h1 class="tracking-tighter mb-3 text-white font-semibold shadow-sm text-3xl" >Edit Your Post</h1>
            <textarea placeholder="what's new your mind ?" class="w-1/3 mt-3 p-4 rounded-md block outline-none border-2 resize-none border-zinc-800 bg-transparent" name="content"><%= post.content %></textarea>
            <input class="px-4 py-2 rounded-lg bg-yellow-500 hover:bg-yellow-600  font-semibold text-sm block mt-3" type="submit" value="Edit Post">
        </form>
    </main>
</body>
</html>